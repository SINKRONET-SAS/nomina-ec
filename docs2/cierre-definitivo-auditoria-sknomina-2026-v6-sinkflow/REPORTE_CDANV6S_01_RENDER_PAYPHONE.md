# CDANV6S-01 Render y PayPhone

Resultado: cerrado previo.

- `render.yaml` ejecuta `npm run seed:admins` dentro de `buildCommand`.
- `render.yaml` declara variables PayPhone productivas necesarias: base API, token, store id, provider, webhook secret y timeout.
- No se detecta necesidad de parche runtime en esta fase.
