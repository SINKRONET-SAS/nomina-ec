# ANECV6R26-01 - CONTRASTE FORENSE

Objetivo:

- demostrar con codigo real si V6 aplica o no a `sinkroniq-mobile`.

Checks minimos:

- leer `AuditoriaNominaEC2026V6.jsx`;
- confirmar re-export a V65;
- confirmar que la fuente explicita `nomina-ec publico`;
- contrastar `render.yaml`, `mobile/package.json` y rutas SKNomina reales.
