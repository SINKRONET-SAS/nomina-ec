# GPA26-04 - QA release

Ejecutar gates backend, Prisma, contratos, build web, UTF-8 sin BOM y `git diff --check`. Actualizar reporte, `.github/CODEX_CONTEXT.md`, `.vscode/AuditLock.json`, commit con `phase: GPA26-04 task: gestion-periodos-anuales` y push.
