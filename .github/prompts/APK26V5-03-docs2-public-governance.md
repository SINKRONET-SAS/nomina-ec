# APK26V5-03 Gobierno Docs2

Agrega `docs2/` a `.gitignore` y retira `docs2/` del tracking publico con `git rm -r --cached docs2/`, sin borrar los archivos locales. La trazabilidad minima debe quedar en `.github/CODEX_CONTEXT.md`, prompts y AuditLock.
