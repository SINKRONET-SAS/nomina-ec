# TFD26-04 QA Release

Aplicar `RULES.md` y `CODEX_CONTEXT.md`. Ejecutar pruebas backend focalizadas, validar Prisma, compilar frontend, verificar UTF-8 sin BOM, `git diff --check`, actualizar `AuditLock.json`, commit con `phase: TFD26 task: 04` y push a `main`.
